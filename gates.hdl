import (
    "builtin"
)

chip And(a wire, b wire) (out wire) {
    tmp := builtin.Nand(a, b)
    res := builtin.Nand(tmp, tmp)
    return res 
}

chip AndThreeWay(a wire, b wire, c wire) (out wire) {
	tmp := And(a, b)
	res := And(tmp, c)
	return res
}
